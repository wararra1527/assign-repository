from sklearn.ensemble import RandomForestClassifier
rf = RandomForestClassifier(n_estimators= 200,
                            max_depth = 3,
                            random_state = 0)
rf.fit(X_dum, y)

y_pred = rf.predict(X_dum)

from sklearn.metrics import f1_score
f1_score(y, y_pred)
# Out[22]: 0.7292993630573249